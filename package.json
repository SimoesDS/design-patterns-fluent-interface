{
  "name": "design-patterns-fluent-interface",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --config jest.config.mjs",
    "test:cov": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --config jest.config.mjs --coverage",
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --config jest.config.mjs --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
